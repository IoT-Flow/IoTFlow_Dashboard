# Admin User Management - Visual Guide

## 🎨 UI Flow

### 1. Admin Sidebar Menu
```
┌─────────────────────┐
│ IoTFlow Dashboard   │
├─────────────────────┤
│ 📊 Dashboard        │  (Admin view)
│ 📱 Devices          │
│ 📈 Telemetry        │
│ 👤 Profile          │
├─────────────────────┤
│ Admin Only:         │
│ 📡 MQTT             │
│ 👥 Users            │
└─────────────────────┘

Note: Device Control removed Dec 10, 2025
```

### 2. Users Management Page Layout

```
┌────────────────────────────────────────────────────────────────────────────┐
│  User Management                                              🔄 Refresh   │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  🔍 Search users...                    [All] [Active] [Inactive]          │
│                                                                            │
├────────────────────────────────────────────────────────────────────────────┤
│  Username │ Email              │ Role     │ Status   │ Created   │ Actions│
├───────────┼────────────────────┼──────────┼──────────┼───────────┼────────┤
│  admin    │ admin@iot.com      │ 🔵 Admin │ ✅ Active │ 1/1/2025  │ 👁️ 👤 🔄│
│  user1    │ user1@iot.com      │ ⚪ User  │ ✅ Active │ 1/2/2025  │ 👁️ 👤 🔄│
│  user2    │ user2@iot.com      │ ⚪ User  │ ❌ Inactive│ 1/3/2025  │ 👁️ 👤 🔄│
│  johndoe  │ john@example.com   │ ⚪ User  │ ✅ Active │ 1/4/2025  │ 👁️ 👤 🔄│
└────────────────────────────────────────────────────────────────────────────┘

Actions Legend:
👁️  View Devices - Opens modal showing user's registered devices
👤  Toggle Role - Promote to Admin / Demote to User
🔄  Toggle Status - Activate / Deactivate user
```

### 3. View Devices Modal

```
┌─────────────────────────────────────┐
│  user1's Devices              × Close│
├─────────────────────────────────────┤
│                                     │
│  📱 Temperature Sensor 1            │
│     Device ID: TEMP_001             │
│  ─────────────────────────────────  │
│  📱 Humidity Sensor                 │
│     Device ID: HUM_002              │
│  ─────────────────────────────────  │
│  📱 Smart Thermostat                │
│     Device ID: THERM_003            │
│                                     │
├─────────────────────────────────────┤
│                          [Close]    │
└─────────────────────────────────────┘
```

## 🔄 User Interactions

### Action Flow: Promote User to Admin

```
┌──────────────┐
│ Admin clicks │
│   👤 icon    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────┐
│ Toast: "Updating user role..."   │ ← Loading state
└──────┬───────────────────────────┘
       │
       ▼  API Call: PUT /api/users/2/role
       │              { is_admin: true }
       │
       ▼
┌──────────────────────────────────┐
│ Toast: "User promoted to admin   │ ← Success
│         successfully"             │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Table updates: ⚪ User → 🔵 Admin│ ← Local state update
└──────────────────────────────────┘
```

### Action Flow: Deactivate User

```
┌──────────────┐
│ Admin clicks │
│   🔄 icon    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────┐
│ Toast: "Updating user status..." │ ← Loading state
└──────┬───────────────────────────┘
       │
       ▼  API Call: PUT /api/users/2/status
       │              { is_active: false }
       │
       ▼
┌──────────────────────────────────┐
│ Toast: "User deactivated          │ ← Success
│         successfully"             │
└──────┬───────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│ Table updates: ✅ Active →❌Inactive│ ← Local state update
└──────────────────────────────────┘
```

## 🔒 Security Flow

### Authorization Check (Backend)

```
┌─────────────────────┐
│ Request arrives     │
│ GET /api/users      │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────────────────────┐
│ verifyToken middleware              │ ← JWT validation
│ Attaches user to req.user           │
└─────────┬───────────────────────────┘
          │
          ▼
┌─────────────────────────────────────┐
│ Controller: getAllUsers()           │
│                                     │
│ if (!req.user.is_admin) {          │ ← Admin check
│   return res.status(403).json({    │
│     error: 'Admin access required' │
│   });                               │
│ }                                   │
└─────────┬───────────────────────────┘
          │
          ▼ (if admin)
┌─────────────────────────────────────┐
│ Query database                      │
│ Return users (exclude password_hash)│
└─────────────────────────────────────┘
```

### Self-Modification Prevention

```
PUT /api/users/1/role  (User 1 trying to change own role)
     │
     ▼
┌─────────────────────────────────────┐
│ if (req.user.id === userId) {      │ ← Self-check
│   return res.status(403).json({    │
│     error: 'Cannot modify your own │
│              admin status'          │
│   });                               │
│ }                                   │
└─────────────────────────────────────┘
```

## 📊 State Management

### Frontend State Flow

```
┌──────────────────────────────────────────────────────────────┐
│ UsersManagement Component                                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  [users] ← Full user list from API                          │
│  [filteredUsers] ← Filtered by search + status              │
│  [searchQuery] ← Search input value                         │
│  [statusFilter] ← 'all' | 'active' | 'inactive'            │
│  [selectedUser] ← Currently viewing devices for             │
│  [userDevices] ← Devices of selectedUser                    │
│  [loading] ← API call in progress                           │
│                                                              │
│  useEffect(() => loadUsers(), [])  ← On mount               │
│  useEffect(() => filterUsers(), [users, searchQuery, ...])  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 🎯 API Endpoints Summary

```
┌─────────────────────────────────────────────────────────────┐
│ Backend API Routes                                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  GET    /api/users           → List all users              │
│  GET    /api/users/:id/devices → Get user's devices        │
│  PUT    /api/users/:id/role  → Update is_admin field       │
│  PUT    /api/users/:id/status → Update is_active field     │
│                                                             │
│  All require:                                               │
│  - Authorization: Bearer <JWT>                              │
│  - req.user.is_admin === true                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 📱 Responsive Behavior

### Desktop (>960px)
```
┌──────────────────────────────────────────────────────────────────┐
│ Sidebar (240px) │ Main Content (rest of screen)               │
│                 │                                              │
│ 📊 Overview     │  User Management                             │
│ 📱 Devices      │  ┌────────────────────────────────────────┐  │
│ 👥 Users        │  │ Search + Filters                       │  │
│ ⚙️  Admin       │  └────────────────────────────────────────┘  │
│                 │  ┌────────────────────────────────────────┐  │
│                 │  │ User Table (full width)                │  │
│                 │  │ All columns visible                    │  │
│                 │  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

### Mobile (<600px)
```
┌──────────────────────────────┐
│ ☰ Menu  IoTFlow Dashboard    │
├──────────────────────────────┤
│ User Management              │
│                              │
│ 🔍 Search...                 │
│ [All][Active][Inactive]      │
│                              │
│ ┌──────────────────────────┐ │
│ │ admin                    │ │
│ │ admin@iot.com            │ │
│ │ 🔵 Admin  ✅ Active      │ │
│ │ 👁️ 👤 🔄                 │ │
│ └──────────────────────────┘ │
│                              │
│ ┌──────────────────────────┐ │
│ │ user1                    │ │
│ │ user1@iot.com            │ │
│ │ ⚪ User  ✅ Active        │ │
│ │ 👁️ 👤 🔄                 │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘
```

## 🎨 Color Scheme

```
Role Badges:
🔵 Admin - Blue (#1976d2)
⚪ User  - Grey (#757575)

Status Badges:
✅ Active - Green (#2e7d32)
❌ Inactive - Grey (#757575)

Action Buttons:
👁️  View - Default grey
👤  Toggle Role - Blue when admin, grey when user
🔄  Toggle Status - Green when active, grey when inactive
```

## 🚀 Performance Optimizations

```
1. Local State Updates:
   ├─ After API success, update local state immediately
   ├─ No need to reload entire user list
   └─ Instant UI feedback

2. Lazy Loading:
   ├─ User devices loaded only when modal opens
   ├─ Not preloaded with user list
   └─ Reduces initial API payload

3. Debounced Search:
   ├─ Filter runs on client side
   ├─ No API calls during typing
   └─ Fast, responsive filtering

4. Memoization Potential:
   ├─ filterUsers() could use useMemo
   ├─ Prevents unnecessary recalculations
   └─ Future optimization if needed
```

---

**UI Framework**: Material-UI (MUI) v5
**Design Pattern**: Master-Detail with modal dialogs
**Responsiveness**: Mobile-first with Material-UI Grid & breakpoints
